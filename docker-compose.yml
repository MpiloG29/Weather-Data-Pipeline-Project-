version: '3.8'

services:
  weather-pipeline:
    build: .
    container_name: weather-pipeline
    volumes:
      - ./data:/app/data
      - ./output:/app/output
    environment:
      - DATABASE_URL=sqlite:///data/weather_data.db
    command: python run_pipeline.py 1